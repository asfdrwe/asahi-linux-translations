[Progress Report: Linux 7.2](https://asahilinux.org/2026/08/progress-report-7-2/)の非公式日本語訳です。

まだ作業中

訳注:
Think different は [Apple の広告キャンペーンスローガン](https://ja.wikipedia.org/wiki/Think_different)です。『違うことを考えよう』『常識にとらわれずに考えよう』等の意味ですが、訳すとニュアンスが伝わらないので原文のままにしています。当時の日本の広告でも原文のままだったはずです。

---
# 進捗報告：Linux 7.2

- [前回](https://github.com/asfdrwe/asahi-linux-translations/blob/main/PROGRESS202606.md)

Linux 7.2 がリリースされました！ あっという間でしたね。また Asahi Linux の進捗報告をお届けします。今日は興味深い進展がたくさんあるので、お茶でも淹れてゆっくり楽しんでください。

### Think Different… 再び
Apple Silicon プラットフォームの電源管理インフラは複雑です。役割は [SMC](https://asfdrwe.github.io/asahi-linux-translations/hw/soc/smc/)、[PMGR](https://asfdrwe.github.io/asahi-linux-translations/project/glossary/#p)、[PMP](https://asfdrwe.github.io/asahi-linux-translations/project/glossary/#p) (訳注:リンク追加)など複数のハードウェアブロックに分かれており、これらはすべてこのブログで以前取り上げたことがあります(訳注:例えば[これ](https://github.com/asfdrwe/asahi-linux-translations/blob/main/PROGRESS202604.md#%E3%82%A8%E3%83%8D%E3%83%AB%E3%82%AE%E3%83%BC%E5%95%8F%E9%A1%8C%E3%82%92%E8%A7%A3%E6%B1%BA))。これらのブロックに対応することは消費電力にとって重要ですが、バッテリー寿命を改善する上で最大の障害の一つは、アプリケーションコア自体でした。

CPU コアを『スリープ』させる方法は複数あり、それぞれ特定の文脈で使用されなければなりません。ARM CPU コアをスリープさせる最も基本的な方法は、Wait For Interrupt（WFI）命令を使用することです。これは、割り込みソースからの割り込みによって起こされるまで、コアに何もせず停止するよう指示します。これによりコアがコードを実行しなくなるため電力を節約できますが、コアは電源が入ったままになり、極めて迅速に作業を再開できるだけの状態を保持します。そのため、WFI は通常、稼働中のシステムでコアを止めるためにのみ使用されます。Apple のコアには『深い(deep)』WFI モードがあり、状態を失う代わりにコアのより多くの部分をシャットダウンします。私たちの下流の cpuidle ドライバは、このモードで WFI を設定し、コアの状態を保存してから WFI ループを発行することで動作します。

このようなベンダー固有の電源管理の奇妙さはよくあることです。幸いカーネルメンテナにとっては、それらに対処する標準的な方法があります。それが Power State Coordination Interface (PSCI) です(訳注: PSCIの詳細は[ARM の仕様書](https://support.arm.com/documentation/den0022/latest/)を参照)。PSCI は、オペレーティングシステムがシステムファームウェアによって実装された定義済みの CPU コア電源管理機能のセットを呼び出すための標準インターフェースを定義しており、スリープの準備も含まれます。

Linux カーネル内でベンダー固有の電源管理ハックが蔓延するのを避けるため、arm64 アーキテクチャ固有コードのメンテナは、すべてのアップストリームハードウェアが電源管理に PSCI を*必ず*使用しなければならないと義務付けています。そのため、私たちは Apple 固有の cpuidle ドライバをアップストリームできません。ではなぜまだそれを使っているのでしょうか？

PSCI は、ファームウェアへの呼び出しをカーネルからディスパッチするための『コンジット(conduit、配管)』を定義しています。現在カーネルが対応している 2 つのコンジットは、SMC（Secure Monitor Call）と HVC（Hypervisor Call）命令で、これらはより高い例外レベル(Exception Level, EL)に実行を譲るために使用されます。Linux カーネルは EL2 で動作することを想定しているため、その PSCI 呼び出しは EL3 で動作するファームウェアに譲らなければなりません。しかし、Apple のコアは EL3 を実装していません…

カーネルがすでに EL2 で動作しており、話しかけるべき EL3 で動作するファームウェアがないため、少し行き詰りました。Linux は SMC や HVC 命令を発行できません。EL3 に実行を譲る先がないからです。つまり PSCI を利用できません。CPU コアを適切に電源管理できることはバッテリー寿命と効率にとって不可欠なので、現状維持では済みません。速いけど汚い解決策の一つは、m1n1 にカーネルを EL1 にロードさせ、EL2 で PSCI 実装をホストさせることです。理論的には動作しますが、仮想化などの多くのアーキテクチャ機能を壊してしまいます。他に何かできることがあるはず…

考えてみれば、m1n1 は Apple Silicon 向けの私たちの独自ファームウェアのようなものです。mBoot（旧 iBoot）がそれを EL2 で起動し、仕事を終えると、添付されたペイロードにジャンプします。m1n1 は自身のためにメモリを予約せず、常駐しなければならないコードもないため、ペイロードはそのメモリを再利用して上書きできます。

実際の Asahi Linux システムでは、m1n1 はカーネルを直接ではなく U-Boot をロードします。これは U-Boot の UEFI 実装を利用するためで、ディストリビューションやユーザーが好みの標準 UEFI ブートローダー（GRUB、systemd-boot など）を使えるようにします。UEFI はもう一つの機能、Runtime Services も提供します。昔の BIOS 割り込みのように、UEFI Runtime Services はオペレーティングシステムがシステムファームウェア由来のコードにアクセスする方法を提供します。

Arm が公開した PSCI 標準を読むと、API を特定の配管に言及せずに意図的に定義し、SMC と HVC を例として挙げているだけであることに気づきます。これを広く解釈すれば、仕様では*他の*コンジットも許可されていると結論づけられます…

この目的のために、Sven 氏は UEFI Runtime Service ベースの PSCI コンジットの実装に取り組んできました。m1n1 のメモリ領域を他のファームウェア領域のように切り出せば、カーネルは同じ 例外レベルで動作していても、PSCI サービスのために m1n1 にコールバックできるようになります。Sven 氏はすでに m1n1 を修正してメモリを予約し、PSCI 実装を残すようにしており、その使用を可能にするカーネルへのパッチはすでに RFC としてメーリングリストに出ています！

### もう Think Different はやめよう
cpuidle の状況がつい最近まで進展がなかったことを考えると、この分野での作業を触媒した何かの出来事があったと仮定するかもしれません。その通りです。

ARM 仕様では、WFI ループ中のコアはすべての状態を保持すべきだと義務付けています。ですが、これは Apple Silicon ではデフォルトのモードではありません。M1 から M3 シリーズの SoC では、状態保持は chicken bits(訳注:[特定の機能を無効にしたり有効にしたりするための設定ビット](https://asfdrwe.github.io/asahi-linux-translations/project/glossary/#c))を使ってコアごとに設定できます。

言及する価値のない理由により、Apple は現在、M4 シリーズから各コアの chicken bits を mBoot で設定し、それらを制御するレジスタをロックダウンしています。これにより m1n1 の仕事がわずかに減るため少し楽になりますが、低レベルの CPU 動作を微調整できなくなることも意味します。これは特に M4 で問題で、WFI を呼び出すとコアが状態を失い、そこで実行されていたものがクラッシュします。

Yureka 氏は M4 の bringup 作業中にこれに気づき、idle ループの動作を設定可能にするカーネルコマンドラインパラメータを追加しました。このパラメータにより、カーネルに idle ループでコアをどう駐車させるかを指示でき、基本的な no-op ループを行うことも含まれます。これにより、cpuidle ドライバがロードされる前の早期カーネル初期化中に M4 マシンがクラッシュするのを防ぎます。ドライバが引き継ぐと、失われた状態を保存してから WFI を発行します。これを有効にするパッチはすでに linux-next に入っています。

### Apple は Think Different をやめない

Apple はプラットフォームセキュリティの評判を非常に真剣に受け止めています。そのため、エコシステム内の脆弱性を悪用することを最も洗練された攻撃者以外には実行不可能にする機能に、多くのエンジニアリング努力が注がれています。そのような機能の一つが Secure Page Table Monitor、SPTM です。

伝統的に、オペレーティングシステムカーネルはメモリ管理を直接担当してきました。これにはメモリ割り当ての処理、仮想から物理アドレスへのマッピング、MMU/IOMMU 管理が含まれます。これらの操作を担当するコードの脆弱性は、攻撃者にプラットフォームのアドレス空間全体へのアクセスを与える可能性があります。つまり、詰みです。

当然ながら、これによりメモリ管理コードは攻撃者の非常に一般的な標的となり、その仕事が根本的に安全でない（アプリケーションはほぼ何にでも任意の割り当てを望む可能性がある）ことを考えると、正しくロックダウンするのは信じられないほど困難です。

何年も前に、Apple は XNU に Page Protection Layer(PPL) を導入しました。PPL は Apple のハードウェアセキュリティ機能を使用して、ページテーブル管理をカーネルの残りの部分からハードウェアレベルで分離します。これは非常にうまく機能しましたが、攻撃者は最終的に追いつき、PPL 内に侵入してシステムへのフルアクセスを可能にする方法を見つけました。

Apple は過去に IOMobileFramebuffer でも同様の問題を抱えていました。[以前のブログ投稿]()で述べたように、Apple は（ほとんど）IOMFB の問題を、IOMMU の背後にある DCP のファームウェア内に置くことで解決しました。macOS のユーザー空間も XNU も、定義された IPC 関数のセットを介する以外には到達できません。このアプローチからヒントを得て、PPL は最終的に SPTM に進化しました。SPTM は PPL を取り、Apple の Guarded Execution Framework（GXF）内に配置します。GXF は標準の ARM64 Exception Level と並行して動作する Exception Level のセットです。GXF にはまた、CPU が GL1 または GL2 でコードを実行しているときに使用されるカスタムページテーブル権限システムである SPRR も付属しています。

現代の Apple Silicon デバイスが起動すると、iBoot または mBoot は設定されたブートペイロードが XNU イメージかどうかを検出します。もしそうなら、まず SPTM を GL2 にロードします。そこで SPTM はページテーブルとメモリ管理を設定し、それらの機能の制御を GL2 にロックダウンします。その後 XNU が動作し、別の IPC プロトコルを使って SPTM と通信します。XNU が SPTM に正常に接触できない場合、初期化の非常に早い段階でパニックしてシステムを停止します。

これは私たちにとって問題です。m1n1 ハイパーバイザの下で XNU を実行しようとすると、SPTM がロードされていないためクラッシュします。mBoot を m1n1 を XNU バイナリとして扱うように設定すると、m1n1 が必要な方法でメモリを管理できなくなるためクラッシュします。

SPTM は M4 以降で XNU に必須であるため、ハイパーバイザはこれらのマシンで完全に機能しなくなっていました。しかし私たちは諦めないので、それは「was」であって「is」ではないのです！

SPTM 自体は特に特別なものではありません。さまざまなコプロセッサ用の OS 固有ファームウェアブロブと同じ preboot ディレクトリに存在する ARM64 Mach-O バイナリです。つまり、理論的には m1n1 にそれを*すでにハイパーバイザの下で*ロードさせ、隣に XNU をロードし、*両方*を監視できます！しかし SPTM は*必ず*SPRR を有効にした GL2 内で実行されなければなりません。もし私たちが[両方の](https://blog.svenpeter.dev/posts/m1_sprr_gxf/)[仕組みを知って](https://asahilinux.org/docs/hw/cpu/sprr-gxf/)いれば…

Sven 氏が Asahi Linux が始まった頃に SPRR と GXF をリバースエンジニアリングしたおかげで、彼は最近 m1n1 ハイパーバイザにそれらをエミュレートする方法を教えられました！これにより、Apple の SPTM ブロブを XNU が期待する正確な方法でロードし、XNU バイナリに少し手術を施してロードし、M1 から M3 までと同様に MMIO アクセスを監視できるようになります！Sven 氏がこれを達成した詳細な方法により、これらのマシンではトレーシングが遅くなりますが、使えなくなるほどではありません。これにより、近い将来も新しいハードウェアの bringup を続けられるようになります！

### M3 のさらなる進捗！

Asahi Linux を M3 シリーズマシンに持ってくる作業も進んでいます。

ウェブカメラのイメージシグナルプロセッサはほぼ変更がなく、M3 Max でのみスキップされた初期化メッセージが 1 つあるだけでした。chaos_princess 氏がそれを Linux ドライバにサポートを追加し、それにより内蔵ウェブカメラを持つすべての M3 シリーズデバイスでフルウェブカメラサポートが有効になりました。

内蔵マイクも少し変更されました。M3 シリーズデバイスで新しいのは「High Frequency」デシメータで、新しい係数セットとずっと大きな初期化メッセージが必要です。またしても chaos_princess 氏がすぐにこれを解明し、装備されたすべての M3 シリーズデバイスにマイクサポートをもたらしました。

ATCPHY — USB Type-C ポート上で USB3、DisplayPort、Thunderbolt 接続をネゴシエートする責任を持つハードウェアブロック — も少し変更され、TSMC の N3 プロセスノードへの移行により初期化時に新しいチューナブルのシーケンスが必要になりました。

Apple が単にそのために大規模なアーキテクチャ変更を避けるだろうという私たちの仮説は*ほぼ*真実でしたが、もちろんそのような変更にいくつか遭遇しました…

M1 からベース M3 シリーズのすべてのデバイスは、Apple 固有の Texas Instruments USB ポートコントローラである CD3217（または ACE2）を使用していました。これは I2C バス上にあり、USB デバイスが接続されたときにそれらとネゴシエートします。M3 Pro/Max から、Apple は ACE3 に切り替えました。ACE3 は代わりに SPMI バスを使用し、より多くのリバースエンジニアリングが必要でした。[mildsunrise](https://github.com/mildsunrise/) 氏と chaos_princess 氏の共同努力のおかげで、ACE3 は CD3217 とほぼ同じレジスタセットを持ち、I2C 経由でアドレス指定される代わりに SPMI インターフェースにラップされているだけだと発見しました。SPMI インターフェースと ACE3 自体の両方が現在 Asahi Linux で動作しており、すべての M3 シリーズデバイスに USB 3.0 と Thunderbolt サポートをもたらしています。

*予想していた*大規模な変更は、GPU とディスプレイコントローラの両方のファームウェア ABI でした。AGX と DCP の両方のファームウェアは特定の macOS バージョンとペアになっているため、Apple はリリース間でインターフェースを安定させる必要がありません。これが、私たちが各世代のハードウェアで特定の macOS バージョンを「ターゲット」する大きな理由です。M3 シリーズマシンは macOS 14.8.3 で見つかる ABI をターゲットとし、DCP のサポートは現在、M1 と M2 で使用している既存の macOS 13.5 ABI とほぼ機能パリティに達しています！

これらすべての進展が M3 ですでに達成されたマイルストーンの上に来ていることで、公式リリースを切る準備がほぼ整ったことを喜んで発表します！今後数週間でこれについてもっと言うことがあるので、お楽しみに！

### M4 と M5 のことも忘れずに！

M3 だけの助けにとどまらず、Yureka 氏は M4 とさらには初期の M5 の bringup にも取り組んでいます。M4 の WFI 問題以外に、これらの SoC は macOS 15.x ファームウェアバンドルでの Apple の NVMe コントローラファームウェアの破壊的な変更に苦しんでいました。Yureka 氏と Sven 氏が協力して変更を調査し、m1n1 と Linux の両方で実装したので、現在 M4 と M5 で NVMe が動作しています！Yureka 氏はまた、PCIe を Linux がバス上のデバイスを列挙できる状態にまで動作させ、複数の CPU コアが有効な場合に起動直後に Linux がクラッシュする問題を修正しました。M4 と M5 では他にあまり動作していないので、まだ Asahi Installer で有効にする準備はできていませんが、いつものように適切な時期にもっと言うことがあるでしょう。

### デスクトップ動画は難しい

前回、Apple Video Decoder（AVD）の予備サポートを発表しました。このハードウェアブロックは M1 と M2 で H.264（AVC）、H.265（HEVC）、VP9 のビデオデコードを加速し、M3 マシン以上では AV1 デコードも追加します。それ以来、AVD サポートは sofus 氏によってさらに洗練され、AVC、HEVC、VP9 がすべて Asahi Linux がサポートするすべてのマシンでほぼ信頼性高く動作するようになりました！現在はデスクトップ統合を検討する段階にあり、そこで物事が難しくなり始めます…

AVD ハードウェアは根本的にステートレスで、エンコードされたフレームを受け取り、ビデオバッファに変換するだけです。ハードウェアはビットストリーム解析、デコードセッション追跡、またはデコードパイプラインの他の管理を一切行いません。これはそのようなデコーダを念頭に設計された V4L2 Stateless API に適しています。この API は 10 年以上前にアップストリームカーネルに初めて導入されましたが、ユーザー空間は埋め込みデバイス向けの専門ソフトウェア以外では採用が遅れていました。GStreamer は V4L2 Stateless の基本サポートを持っていますが、FFmpeg（およびそれを消費するものすべて）はアウトオブツリーのパッチなしではサポートしていません。ウェブブラウザなどのデスクトップクラスのソフトウェアは、歴史的に VA-API、NVDEC、VDPAU に焦点を当ててきました。最近では、デスクトップでの努力が Vulkan Video に移行し始めています。これにより、V4L2 Stateless はデスクトップソフトウェア向けに始まる前に事実上放棄された状態になっています。

しかし、希望は失われていません。VA-API は AMD と Intel の両方がビデオ加速ハードウェアに採用したおかげで、デスクトップクラスのソフトウェアでほぼ普遍的になっています。V4L2 Stateless ハードウェアがそのようなソフトウェアで使用不能になるのを防ぐため、Bootlin は VA-API から V4L2 Stateless への翻訳レイヤーを作成しました。残念ながらこれはかなり前から放棄されており、パッチなしではビルドできなくなっています。しかし [megi によるより最近の努力](https://xff.cz/git/libva-v4l2_request/) はビルドでき、sofus 氏がそれをフォークして AVD 向けに動作する状態にしました。この翻訳レイヤーをインストールし、ログインセッション用に環境変数を設定すれば、VA-API サポートを実装したソフトウェアが AVD を使ってビデオデコードを加速できるようになります！これはまだ Fedora Asahi Remix ではデフォルトで出荷されておらず、Firefox のビデオデコードサンドボックスでは動作しませんが、まもなく出荷可能なものを用意できることを願っています。

### ダイレクトスキャンアウトへの道

ハードウェア加速ビデオデコードの伝統的な利点は、CPU 負荷の削減でした。これだけでももちろん非常に重要ですが、ビデオデコード中に電力と時間が無駄になっている他の場所があります。まず、CPU はデコードされたフレームデータを GPU メモリにコピーしなければなりません。次に GPU はコンポジタの指示に従ってそのフレームをシーンに合成します。最終的にレンダリングされたシーンはディスプレイコントローラのメモリにコピーされ、ディスプレイコントローラがシーンをスキャンアウトするようプログラムされなければなりません。これらすべてが再生中のビデオのフレームレートで起こらなければなりません。

私たちはこれより良くできます。Linux の DMA サブシステムは複数のデバイス間でメモリ領域を共有することをサポートしています。この文脈では、同じフレームバッファデータの複数のコピーをある領域から別の領域へ行うのを排除できます。ただし、チェーン内のすべてのハードウェアブロックが同じフレームバッファ形式をサポートする場合にのみ機能します。ここで物事が難しくなり始めます。

フレームバッファはあらゆる形式とサイズで来ます。例えばビデオデータはほぼ常に [セミプレーナ](https://en.wikipedia.org/wiki/Planar_(computer_graphics)) [Y’CbCr](https://en.wikipedia.org/wiki/YCbCr) 形式で保存・伝送され、これは 20 世紀のアナログビデオ保存・放送標準に触発されたものです。その上、グラフィックスハードウェアはほぼ常に何らかの特殊なアドレッシングを使用します。ピクセルはメモリ上で「隣り合って」おらず、代わりに [タイル化](https://en.wikipedia.org/wiki/Z-order_curve) されてキャッシュミスや他のパフォーマンス問題を減らします。さらに、フレームバッファはしばしば圧縮もされ、メモリフットプリントとバスへの圧力をさらに減らします。

つまり、ハードウェアブロック間のフレームバッファコピーを排除するのは簡単ではありません。各ハードウェアブロックはフレームバッファのピクセル形式、アドレッシングモード、適用された圧縮について合意しなければなりません。合意できない場合、コピーと変換をソフトウェアで行う必要があります。

なぜそのような「共有」フレームバッファを「生の」ピクセルデータで作成できないのかと聞きたくなるかもしれません。1920x1080 のフレームバッファに生の 8 ビット ARGB ピクセルデータが含まれる場合を考えてみてください。それは約 8 MiB のデータです。4K では約 32 MiB に膨らみます。コピーの追加ペナルティがなくても、この量のデータを毎秒 30、60、165、または 240 フレームで読み書きするのはメモリバスに*莫大な*負担をかけ、したがって莫大な電力消費になります。たとえ無限に高性能で効率的なメモリバスがあったとしても、チェーン内の各ハードウェアブロックはバス外に十分なローカルキャッシュまたは RAM を持ち、そのようなデータ量を処理する馬力を持たなければなりません。これは実現不可能です。

幸いハードウェアベンダーは同意しており、ほとんどのディスプレイハードウェアの「ファミリー」は同じアドレッシングと圧縮スキームをサポートすることでこの問題を解決しています。これにより、彼らの 3D エンジン、ディスプレイコントローラ、ビデオアクセラレータが、巨大なローカルキャッシュを持ったりメモリバスを絶えず叩いたりせずに、メモリ効率の良いフレームバッファを共有できます。Linux では、各ドライバがサポートする形式を宣言し、ソフトウェアスタックのさまざまな部分が共有バッファに使用する共通形式を交渉して合意します。私たちにとって幸いなことに、Apple はここで*あまりにも* Different に考えることを選びませんでした。まあ、ほとんどは。

Apple ハードウェアは 2 つの主要なアドレッシングと圧縮形式を使用します。AGX（GPU にちなんで名付けられた）と Interchange です。  
AGX 形式は AGX 自体の内部でのみ使用されますが、Interchange 形式は DCP と AVD でもサポートされています。macOS では、これにより AVD と AGX の両方からのフレームバッファのダイレクトスキャンアウトが可能になり、Quartz（macOS のコンポジタ）が日常の多くのシナリオでほぼ何もしないことを機会主義的に選べます。ビデオが再生されていて他に何も起こっていない場合、GPU を完全にシャットダウンでき、コンポジタがする必要があるのは AVD が生成する新しいフレームを DCP が表示していることを確認するだけです。ゲームやアプリケーションがフルスクリーンの場合、コンポジタは単にそのアプリケーション自身のフレームバッファのアドレスを DCP に渡し、自身のすべてのレンダリング活動を無効にできます。良いニュースは、私たちがこれを Asahi Linux で再現できる道を順調に進んでいることです！

Alyssa 氏と Lina 氏が築いた基盤のおかげで、AGX ドライバはすでに Apple の Interchange フレームバッファ形式の基本サポートを持っていましたが、単にまだ配線されていませんでした。Oliver Bestmann 氏と私は、DCP カーネルドライバおよび Asahi と Honeykrisp の両方の Mesa ドライバで Interchange サポートを配線して有効にすることを志願しました。Y’CbCr とオーバーレイプレーンサポートを DCP に持ち込むすでに行われた作業と合わせて、これにより AGX レンダリングされたフレームバッファの DCP へのダイレクトスキャンアウトへの道が開かれました！AVD サポートは進行中で、sofus 氏が現在 Interchange 形式バッファを出力する能力を調査しています。

これらすべてが素晴らしいニュースですが、まだユーザーにこれを出荷する準備はできていません。KDE Plasma のコンポジタである Kwin は、AGX と DCP が別個のハードウェアブロックであるため、私たちのセットアップを「マルチ GPU」と見なします。そのため、DMA-BUF API を使用したダイレクトスキャンアウトは現在完全に無効になっています。Kwin の開発者はこの状況を改善する作業を積極的に進めており、私たちのセットアップのようなものでのダイレクトスキャンアウトは、早ければ Plasma 6.8 から Kwin で有効になる可能性があります！

### 諸々のアップストリーム

いつものように、私たちはパッチをアップストリームし続けています。最近数ヶ月で進捗が遅くなり、後退したようにさえ見えるかもしれません。これは単にほとんどのものをすでにアップストリームしたからです！残っているもののほとんどは、まだアップストリーム*できない*巨大な作業（例：GPU、DCP）か、アップストリームのバックログのほとんどをクリアしたおかげで作業できるようになった*新しい*機能です。とはいえ、ツリーにはまだいくつかの細々したものが残っています。最近アップストリームされたパッチには、speakersafetyd を機能させることに関連する I2S ペリフェラルの変更の数々、SMC ベースの hwmon ドライバを有効にする Devicetree パッチ、そしてもちろん macOS 27 によって導入されたすべての SMC ファームウェアインターフェースの変更の修正が含まれます。

### 改めてありがとう！

頻繁な読者は、最近のブログ投稿に新しい名前がいくつか登場していることに気づいているでしょう。私たちの [GitHub Sponsors](https://github.com/sponsors/AsahiLinux) と [Open Collective](https://opencollective.com/asahilinux) の後援者の寛大な支援のおかげで、これらの人々に魔法を発揮するために必要なハードウェアを提供できました。AVD サポート、M4 bringup、そして私たちが準備している他の良いものは、あなたたちのおかげで可能になっています。

#### James Calligeros · 2026-08-26
